/** @format */
interface GenerateQrOnBannerProps {
    stringToCreateQR: string;
    bannerImg: string;
    bannerText: string;
}
interface GenerateQrResponse {
    qrString: string;
    blob: Blob | null;
    bannerText: string;
}
/**
 * Generate QR code and add in your banner
 * @param props GenerateQrOnBannerProps
 * @returns Promise<GenerateQrResponse>
 */
export declare const generateQrOnBanner: (props: GenerateQrOnBannerProps) => Promise<GenerateQrResponse>;
export {};
